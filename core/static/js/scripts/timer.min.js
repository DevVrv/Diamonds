"use strict";function timer(containerID,clockID,link,timer=60){const buttonHTML=`\n        <div class="login__info w-100 d-flex align-items-center justify-content-between py-3">\n            <span>Didn't get the code ?</span>\n            <a href="${link}" class="btn btn-secondary" id="resend_mail_code">Send the code again</a>\n        </div>\n    `,container=document.querySelector(containerID);let clock=null;void 0!==container&&(clock=container.querySelector(clockID));const time=Number(Date.now());function updateClock(){const timeNow=Number(Date.now()),timeFromStorage=Number(localStorage.getItem("timer"));let difference=Math.floor(timeNow/1e3-timeFromStorage/1e3);if(difference>=timer){let clockParent;localStorage.clear(),clock.parentElement.remove(),container.innerHTML=buttonHTML}else clock.textContent=timer-difference}if(null===localStorage.getItem("timer")&&localStorage.setItem("timer",time),null!==clock){let timerID=setInterval(updateClock,1e3)}}