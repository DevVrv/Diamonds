"use strict";function timer(timerContainer="#resend_timer_container",timerSelector="#resend_timer",resend_link=String){const resendButton=`\n        <div class="login__info w-100 d-flex align-items-center justify-content-between py-3">\n            <span>Didn't get the code ?</span>\n            <a href="${resend_link}" class="btn btn-secondary" id="resend_mail_code">Send the code again</a>\n        </div>\n    `,timer_container=document.querySelector(timerContainer),timer_element=timer_container.querySelector(timerSelector);if(timer_element){const timer_parent=timer_element.parentElement;let timer_value=Number(timer_element.textContent);const timerId=setInterval(()=>{timer_value>0?timer_element.textContent=timer_value-=1:timer_parent&&(timer_container.insertAdjacentHTML("beforebegin",resendButton),timer_parent.remove(),clearInterval(timerId))},1e3)}}